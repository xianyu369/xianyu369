spring:
  datasource:
    driver-class-name: dm.jdbc.driver.DmDriver
    username: SYSDBA
    password: 021910wenD
    url: jdbc:dm://localhost:5236/ALIYUN
mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

logging:
  level:
    com.example.aliyun: debug